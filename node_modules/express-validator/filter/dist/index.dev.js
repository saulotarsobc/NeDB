"use strict";

var _require = require('./sanitization-chain-builders'),
    buildSanitizeFunction = _require.buildSanitizeFunction,
    sanitize = _require.sanitize,
    sanitizeBody = _require.sanitizeBody,
    sanitizeCookie = _require.sanitizeCookie,
    sanitizeParam = _require.sanitizeParam,
    sanitizeQuery = _require.sanitizeQuery;

module.exports = {
  buildSanitizeFunction: buildSanitizeFunction,
  sanitize: sanitize,
  sanitizeBody: sanitizeBody,
  sanitizeCookie: sanitizeCookie,
  sanitizeParam: sanitizeParam,
  sanitizeQuery: sanitizeQuery,
  matchedData: require('./matched-data')
};