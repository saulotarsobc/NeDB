"use strict";

var _require = require('./constants'),
    extraSanitizers = _require.extraSanitizers,
    extraValidators = _require.extraValidators;

exports.isSanitizer = function (name) {
  return name.startsWith('to') || extraSanitizers.includes(name);
};

exports.isValidator = function (name) {
  return name.startsWith('is') || extraValidators.includes(name);
};